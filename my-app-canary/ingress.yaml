apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app: my-app-canary
  name: my-app-canary
  namespace: my-app-canary
spec:
  rules:
  - host: my-app-canary.local
    http:
      paths:
      - backend:
          service:
            name: my-app-canary-stable
            port:
              name: http
        pathType: ImplementationSpecific

