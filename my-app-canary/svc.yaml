apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-app-canary
  name: my-app-canary
  namespace: my-app-canary
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: my-app-canary
