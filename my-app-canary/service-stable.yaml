apiVersion: v1
kind: Service
metadata:
  name: my-app-canary-stable
  namespace: my-app-canary
  labels:
    app: my-app-canary
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: my-app-canary
